import window  # 导入名为window的模块，可能是一个自定义的窗口处理库
import cv2     # 导入OpenCV库，用于图像处理和计算机视觉任务

window.showStr("123+321")  # 调用window模块中的showStr函数，显示字符串"123+321"
cv2.waitKey(0)            # 使用OpenCV库的waitKey函数等待用户按键输入，参数0表示无限等待直到用户按下任意键
